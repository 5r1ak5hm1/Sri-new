#include <stdio.h>
int isPrime(int num) {
    if (num <= 1) return 0;
    for (int i = 2; i * i <= num; i++) {
        if (num % i == 0) return 0;
    }
    return 1;
}
int main() {
    int start, end;
    printf("Enter start and end of interval: ");
    scanf("%d %d", &start, &end);
    printf("Primes between %d and %d: ", start, end);
    for (
