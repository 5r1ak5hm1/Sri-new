#include <stdio.h>

struct Employee {
    int id;
    char name[20];
};

int main() {
    struct Employee emp[2] = {{1, "John"}, {2, "Alice"}};
    for (int i = 0; i < 2; i++) {
        printf("ID: %d, Name: %s\n", emp[i].id, emp[i].name);
    }
    return 0;
}
